overwrite: true
schema: apollo/schema.graphqls
documents: apollo/queries/*.gql
generates:
  generated/graphql.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
